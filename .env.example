# Supabase Configuration (Client-side - safe to deploy)
# Get these values from your Supabase project settings: https://app.supabase.com/project/_/settings/api
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Sentry Error Tracking (Optional - for production monitoring)
# Get from: https://sentry.io/settings/projects/your-project/keys/
# VITE_SENTRY_DSN=https://xxxxxxx@sentry.io/xxxxxxx

# ============================================================
# SERVER-SIDE SECRETS (Set in Supabase Edge Function Secrets)
# These should NOT be in .env.local - only set via:
#   supabase secrets set VARIABLE_NAME=value
# ============================================================

# Google Gemini AI Key (platform key for AI generation)
# Get from: https://makersuite.google.com/app/apikey
# supabase secrets set GEMINI_API_KEY=your_gemini_api_key

# Encryption Key (base64-encoded 32 bytes for AES-256-GCM)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# supabase secrets set APP_ENCRYPTION_KEY=your_base64_key

# Gmail OAuth (for email inbox integration)
# Get from: https://console.cloud.google.com/apis/credentials
# Redirect URI: https://your-vercel-origin/
# supabase secrets set GMAIL_CLIENT_ID=your_gmail_client_id
# supabase secrets set GMAIL_CLIENT_SECRET=your_gmail_client_secret

# Outlook OAuth (for email inbox integration) - OPTIONAL
# Get from: https://portal.azure.com (App registrations)
# Redirect URI: https://your-vercel-origin/
# supabase secrets set OUTLOOK_CLIENT_ID=your_outlook_client_id
# supabase secrets set OUTLOOK_CLIENT_SECRET=your_outlook_client_secret
